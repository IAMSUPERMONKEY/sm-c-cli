# 超级猩猩 CLI 安装（更新）指南

以下步骤面向 AI Agent，以后更新时采用和安装同样的步骤。

## 环境要求

开始安装之前，请确保环境中已安装：

- Node.js（npm/npx）

## 安装

```shell
# 安装 CLI
npm install -g sm-c-cli@latest

# 安装 CLI Skills（必需）
npx skills add IAMSUPERMONKEY/sm-c-cli -y -g
``` 
